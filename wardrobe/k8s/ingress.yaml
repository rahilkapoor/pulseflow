# Generated by Containerization Assist using AI
# Kubernetes Ingress manifest for external access to wardrobe application
# Note: Requires ingress-nginx controller to be installed on your AKS cluster

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wardrobe-ingress
  namespace: default
  labels:
    app: wardrobe
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - wardrobe.example.com
    secretName: wardrobe-tls
  rules:
  - host: wardrobe.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wardrobe
            port:
              number: 80
